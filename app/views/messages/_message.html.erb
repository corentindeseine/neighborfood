<div class="message" id="message-<%= message.client.id %>">
  <small>
    <strong><%= message.client.first_name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
